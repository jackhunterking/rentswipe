VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Optional (for using service key server-side only):
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================
# MCP (Model Context Protocol) Credentials
# ============================================
# These credentials are used in ~/.claude/config.json for Claude Code MCP integration
# DO NOT commit these credentials to version control!

# GitHub Personal Access Token (for GitHub MCP Server)
# Create at: https://github.com/settings/personal-access-tokens/new
# Required scopes: repo, read:user, read:org, workflow, read:project
# GITHUB_PERSONAL_ACCESS_TOKEN=github_pat_xxxxxxxxxxxxx

# Supabase MCP Access Token (for Supabase MCP Server)
# Get from: https://app.supabase.com (Dashboard → Account → Access Tokens)
# SUPABASE_MCP_TOKEN=sbp_xxxxxxxxxxxxx
